/**
 * Build-time constants, replaced by Vite's `define` in vite.config.ts.
 *
 * `__NP_PAGES_ORIGIN__` is the origin the sources are written against; `__NP_SITE_ORIGIN__` is
 * the one this build targets. They are equal unless NP_SITE_ORIGIN is set.
 */
declare const __NP_PAGES_ORIGIN__: string;
declare const __NP_SITE_ORIGIN__: string;
