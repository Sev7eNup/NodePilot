# Everything in this folder is GENERATED by deploy/desktop/Build-DesktopInstaller.ps1 from the
# tracked brand asset src/nodepilot-ui/public/appicon.png — never commit the results.
# Explicit, because the root .gitignore ignores *.png but not *.ico, which left this folder in a
# half-ignored state.
*
!.gitignore
