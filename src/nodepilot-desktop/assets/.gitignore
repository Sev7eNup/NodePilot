# Everything in this folder is GENERATED by scripts/generate-desktop-icons.ps1 from the tracked
# brand assets src/nodepilot-ui/public/appicon-<skin>.png — never commit the results.
# Explicit, because the root .gitignore ignores *.png but not *.ico, which left this folder in a
# half-ignored state.
*
!.gitignore
