# Intentionally blank: copied examples must not contain a usable shared credential.
# Generate a unique password before `docker compose up`; Compose fails closed while this is empty.
NODEPILOT_GRAFANA_ADMIN_PASSWORD=
